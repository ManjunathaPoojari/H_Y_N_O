# TODO: Add Back Link to Nutrition Profile Page

## Tasks
- [x] Modify `UserProfile.tsx` to accept `onNavigate` prop and add a back button at the top
- [x] Update `App.tsx` to pass the `navigate` function to the `UserProfile` component
