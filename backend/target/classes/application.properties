# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hyno_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=madhu@9248
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
# IMPORTANT: Using create-drop to ensure tables are recreated with correct VARCHAR(50) column types
# Change back to 'update' after first successful run
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Server Configuration
server.port=8081
# server.servlet.context-path=/

# Logging Configuration
logging.level.com.hyno=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
jwt.secret=mySuperSecretKey1234567890abcdef
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:3001
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=forappleid9248@gmail.com
spring.mail.password=Domin@r9248
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Thymeleaf Configuration for Email Templates
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# Security Headers Configuration
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=strict
server.servlet.session.timeout=30m

# Additional Security Settings
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-binding-errors=never

# CSRF Protection (enabled by default in Spring Security, but ensure)
# spring.security.csrf.enabled=true

# Additional Security Headers
server.servlet.session.cookie.max-age=1800
# X-Frame-Options: DENY (to prevent clickjacking)
# Content-Security-Policy: default-src 'self' (basic CSP)
# These are typically set in SecurityConfig, but placeholders here

# Request Timeouts
server.connection-timeout=20000
server.tomcat.connection-timeout=20000
server.tomcat.keep-alive-timeout=20000
spring.mvc.async.request-timeout=30000

# Rate Limiting (basic, if using Spring Security)
# spring.security.rate-limiter.enabled=true
# spring.security.rate-limiter.requests-per-minute=60
